{
    "rewrites": [
      { "source": "/(.*)", "destination": "/index.html" }
    ],
    
      "build": {
        "env": {
          "NODE_ENV": "production"
        }
      },
      "installCommand": "npm install --force && npm install -g vite"

  }
